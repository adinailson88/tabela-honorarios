# outputs

Diretório reservado para artefatos gerados.

## Situação atual

O arquivo `dashboard_senge_honorarios_tos_valor_municipio_layout_crea.html` ainda permanece na raiz por compatibilidade com o fluxo atual.

O `index.html` também deve permanecer na raiz enquanto o GitHub Pages estiver configurado para publicar a partir da raiz do branch `main`.

## Uso futuro

Esta pasta pode receber, após ajuste e validação do build:

- HTMLs gerados;
- relatórios agregados;
- resumos de auditoria;
- artefatos de validação sem dados individualizados.

Não colocar nesta pasta bases linha a linha ou arquivos com dados individualizados de ARTs.
